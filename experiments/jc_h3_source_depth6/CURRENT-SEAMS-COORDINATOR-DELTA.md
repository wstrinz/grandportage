# JC coordinator delta: newer seams after the GP depth-6 packet

**Date:** 2026-08-01

**Observed local JC head:** `1842331`

**GP head used for this note:** `6b9e6de`

## What changed since the web review head

The web review inspected JC only through `31d36fc`. The local tree already
contains the later R6 frame adjudication and the depth-8 straggler/zero-block
composition. Please use the following scopes in coordinator prose and future
receipts.

### Original-pair to normalized-root seam

Do not say the whole stage is localized to one premise.

Current frontier:

```text
R1  root normalization                 PROVED, bijective over Z[1/3]
R2  depression unit                    PROVED
R3  monic normalization                CHECKED
R4  grading alignment                  PROVED
R5  selected cubic/root support        CHECKED_PREMISE_BOUND
R6  residual window/frame conversion   OPEN, DIAGNOSED
R7  ladder coefficients at (75,125)    INFERRED, IDENTIFICATION UNBOUND
```

The newer R6 result does not supply `J`. It proves that the proposed linear
frame transport is refuted by the landed hull and that no finite universal
`J` is derivable from the printed permitted region. It does not say the actual
residual is unbounded. The missing datum is the exact non-monomial conversion
from the GGV3 frame through the landed reductions to the covered `psi2` frame.

Keep `target_pair_to_normalized_laurent_root` open, along with original-source
membership, reverse lift, coverage, H3, and verdict promotion.

### Corrected recurrence

On the unilateral domain `d >= 6`, the constant-coefficient annihilator ideal
is exactly `(S^8)`. What remains impossible is an annihilator with nonzero
constant term and a reversible backward recurrence.

This is an object-level theorem. Its native checker/Lean formalization, not GP
transport alone, must police the algebra. If a GP adapter is requested, send
the frozen certificate and checker unchanged; it should initially have graph
effect `NONE` and preserve H8 plus first-order/generic conditionality.

### S4 `K`-point scope

Authorized:

- an exact `K`-point exists on `C=0, C2=0`;
- the generic landed slice genuinely needs a quadratic extension;
- 24 declared off-locus seeds have provably nonsquare discriminant.

Still open:

- whether any `K`-point exists on `C=0, C2!=0`;
- confinement of all `K`-points to `C2=0`;
- source membership, component geometry, H3, and verdict promotion.

Please describe the 24-seed result as a bounded search envelope, never an
emptiness or confinement certificate.

### Depth-8 fixed-witness/fiber scope

The new composition shows, conditional on H8 at the first-order layer:

- `c7_4` is solved rather than a rescue direction on the nine-relation locus;
- the cokernel rotates;
- `Omega_comb` is nonzero along the complete free `c8_5` fiber of one landed
  base witness;
- the depth-9 additive pair is not authorized there.

This does not exclude the rest of the 12-dimensional locus or a component.
Base-direction motion, the unreplayed conjugate, nonlinear lifting, source
sufficiency, H3, and the final verdict remain open.

## Proposed GP usage policy

For any change to `TARGET_75_125_STATUS.md`, an execution plan, or a lead
handoff, include one of:

1. a current GP authority ledger and its stated ceiling; or
2. an explicit `OUTSIDE_GP_CURRENT_SEMANTICS` note naming the native verifier
   or Lean theorem that checks the object-level claim.

This does not make GP part of discovery. The operating loop remains:

```text
native discovery
  -> frozen receipt + independent native replay
  -> Lean theorem for stable generic semantics where worthwhile
  -> isolated GP adapter / promotion ledger
  -> shared status update
```

JC agents should continue emitting native receipts, not GP JSON. Useful fields
for the adapter lane are exact model/field/point scope, guards, premises,
direction, mutation refusals, and explicit unsupported conclusions.

## GP-side queue proposed from these receipts

Immediate:

1. bind the R1--R7 decomposition and newer R6 diagnosis;
2. generate a delimited status block from the GP authority ledger.

Next live assay:

3. represent S4 as the covered pieces `C2=0` and `C2!=0`, bind the exact point
   only to the first, and leave the complement open.

Queued pending semantics or another consumer:

4. a Lean-backed unilateral recurrence evidence contract;
5. a Lean-backed jet-linearization evidence contract;
6. constructible-scope visualization after the S4 campaign exists.

No new JC work is required merely to satisfy GP. The current manifests and
checkers are already suitable inputs unless the coordinator changes their
mathematical scope.
