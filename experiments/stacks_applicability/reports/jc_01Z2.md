# Stacks applicability audit: JC-INVERSE-LIMIT-NONEMPTY-AUDIT

- Theorem: [01Z2](https://stacks.math.columbia.edu/tag/01Z2)
- Pinned Stacks commit: `a04446e57ec1fbc252a871afcec7752fb2807b14`
- Statement SHA-256: `3e24bd854e625d820624263fe6f0ab120972e941877ffd01951469058dcab6e1`
- JC context: JC compatible finite truncations and the proposed infinite formal-arc limit
- Intended conclusion: the compatible truncation tower has a limiting point
- Decision: **REFUSED_MISSING_HYPOTHESES**
- Graph effect: **NONE**

## Exact pinned statement

```tex
Let $S = \lim S_i$ be the limit of a directed inverse system
of schemes with affine transition morphisms
(Lemma \ref{lemma-directed-inverse-system-has-limit}).
If all the schemes $S_i$ are nonempty and quasi-compact,
then the limit $S = \lim_i S_i$ is nonempty.
```

## Hypothesis accounting

| Hypothesis | Exact premise | Status | GP binding / reason |
| --- | --- | --- | --- |
| `directed_inverse_system` | $(S_i)$ is a directed inverse system of schemes | **UNSUPPORTED** | The campaign records finitely many exact-affine models, not a typed directed inverse system. |
| `limit_identified` | $S$ is the scheme limit of that system | **MISSING** | No campaign object is proved to be the scheme-theoretic inverse limit of the truncation stages. |
| `transitions_affine` | all transition morphisms are affine | **MISSING** | The truncation transition maps have not been materialized and checked as affine morphisms. |
| `stages_nonempty` | every $S_i$ is nonempty | **OPEN** | Some bounded blocks have solutions over an algebraic closure, but nonemptiness of every stage is not established. |
| `stages_quasi_compact` | every $S_i$ is quasi-compact | **MISSING** | Quasi-compactness is mathematically plausible for affine stages but is not bound as a campaign premise. |

## Application-specific bridge premises

These are needed to use the theorem in this JC context even though they are not hypotheses in the printed Stacks statement.

| Premise | Statement | Status | GP binding / reason |
| --- | --- | --- | --- |
| `limit_point_means_formal_arc` | a point of the scheme limit corresponds to a JC formal arc with the declared source semantics | **MISSING** | The source-to-truncation semantics and point universe have not been composed through an infinite limit. |

## Authority boundary

This sidecar records discovery, source pinning, and premise accounting. It does not create or amend a GP claim, edge, inference, verdict, or kernel rule. Even `READY_FOR_GP_REVIEW` requires an explicit reviewed translation into the campaign graph.
