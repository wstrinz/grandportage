# JC H3 H8 / c7_9 frontier consumer

This is the first bounded consumer of the generic, read-only `frontier/v1`
projection. It links two landed JC updates without changing historical evidence
or graph authority:

- the three P3/P4 receipts discharge the last named H8 transfer premise over
  depths 8 through 15 under P1--P5 and the pin, retaining S2 wherever a
  consumer was already S2-scoped;
- the exact `face(8,1)` identity closes source incidence only on the recorded
  codimension-five `c7_9` family.

The projection therefore removes the H8 qualifier from the exact depth-nine
degree-34 pairing and the operator schedule, while leaving additive residuals,
actual-source membership, H3, and the `(75,125)` verdict untouched. It also
keeps the full `b=0` source-exclusion question open and identifies ranked pin
ablation as its smallest current source-side artifact.

Run the frozen replay:

```text
python experiments/jc_h3_frontier/adapter.py
```

To check the five exact sibling JC receipt bytes and verdict strings without
running any math-stuff release gate:

```text
python experiments/jc_h3_frontier/adapter.py --check-native-bindings
```

The source document is `fixtures/jc_frontier/h8_c79_v1.json`; the compact
checked-in review receipt is `review/jc-h3-h8-c79-frontier-v1.json`.
