# JC H3 low-jet pin-ablation frontier consumer

This consumer binds the coordinator handback and its three exact native
certificates. It records the ranked pin-ablation task as resolved to scoped
results without pretending that the full `b=0` source-exclusion question was
closed.

The positive result is uniform source exclusion for every `c2_2` at
`c2_1=c7_10=0`. The joint `c2_2/c7_10` lane is confined to one exact
hyperplane; at `a=c=1` its generic point and both intercepts are excluded, but
the at-most-130 resultant roots remain open. The residual torus invariant
`J=a*c^(-3)` prevents automatic transport across the full `(a,c)` chart.
Relaxing `c2_1`, `b`, `R`, or `Delta` remains open.

Run the read-only projection:

```text
python experiments/jc_h3_pin_ablation/frontier_adapter.py
```

Its checked-in compact review receipt is
`review/jc-h3-pin-ablation-frontier-v1.json`. Graph effect is `NONE`.
