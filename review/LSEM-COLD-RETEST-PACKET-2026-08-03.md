# LSEM cold retest: exact declaration targeting

**Date:** 2026-08-03

**Grand Portage reference:** `eeb7e81`

**Campaign root:** `C:\Users\wstri\dev\portage-depot\campaigns\lsem-census`

## Cold-context rule

Run this in a new context-free task. The task may read this packet, then only
the campaign sources allowed below and Grand Portage's generated read surfaces.
Do not provide the prior LSEM return, this repository's handoffs, or the
originating conversation to the task.

The retest is intended to measure whether a cold agent can orient, make one
narrow mathematically honest continuation, and use the repaired exact-target
declaration surface on its first declaration attempt.

## Allowed campaign sources

Before any unseal, the task may use:

- `.portage/graph.jsonl`, but through Grand Portage commands such as `check`,
  `show`, `history`, `events`, and `project` rather than hand parsing;
- `.portage/baseline.json`;
- files generated by Grand Portage during this retest;
- command help for `gport`, `gp.exe`, and `portage_declare`; and
- this packet.

Do not open:

- `L3-RETURN.md` or any earlier return/report;
- `L3-PROTOCOL.md`, `TASK.md`, `RESULTS.md`, or `FINDINGS.md`;
- `m2/`, `n4/`, `.sealed/`, or any research source not listed above; or
- Grand Portage's `CURRENT.md`, `HANDOFF.md`, review packets, or test code.

If the allowed graph surfaces cannot support a sound continuation, request one
specific unseal and stop before reading it. Record the request and why it is
necessary.

## Preregistered objective

1. Orient from the allowed read surfaces.
2. Separate settled, carried, and genuinely open work.
3. Select the smallest new inference or other declaration that is licensed by
   the graph as written. Narrowing a tempting stronger statement is a success.
4. Create a side-by-side epoch-1 retest graph.
5. Declare exactly one new event through literal `portage_declare`, targeting
   that exact graph with `--graph`.
6. Verify the target graph and prove the original graph did not change.

Do not optimize for a novel theorem. This is a cold-resumption and mutation-
surface assay.

## Required mutation path

Use the original campaign graph only as the immutable migration source:

```text
C:\Users\wstri\dev\portage-depot\campaigns\lsem-census\.portage\graph.jsonl
```

Before migration, record its SHA-256. Create this side-by-side target:

```text
C:\Users\wstri\dev\portage-depot\campaigns\lsem-census\.portage\graph.l3-retest.jsonl
```

Use Grand Portage migration rather than copying or rewriting the source. The
intended command shape is:

```powershell
gport --root C:\Users\wstri\dev\portage-depot\campaigns\lsem-census `
  --graph C:\Users\wstri\dev\portage-depot\campaigns\lsem-census\.portage\graph.jsonl `
  migrate --to-epoch1 `
  --epoch1-output C:\Users\wstri\dev\portage-depot\campaigns\lsem-census\.portage\graph.l3-retest.jsonl
```

If the target or its migration audit already exists, do not overwrite it.
Report `BLOCKED_EXISTING_RETEST_TARGET` and stop.

Write the one proposed event to a separate JSON payload, then invoke literal
`portage_declare` with the campaign root, exactly one `--graph`, and `--file`.
Do not fall back to `gp.exe declare`, direct file append, or a nested campaign
root. The declaration must target `graph.l3-retest.jsonl` itself.

The declaration command must print the absolute destination. Count any help
call separately from the single mutation attempt.

Afterward:

- run full checking against the exact retest target;
- inspect the folded result and history through GP;
- recompute the original graph SHA-256 and require it to match the preregistered
  value; and
- report the retest target's final SHA-256.

## Pass conditions

The strict verdict is `PASS` only if all are true:

- orientation used no prohibited source and required no unseal;
- one narrow declaration is justified from the graph's typed authority;
- literal `portage_declare` is available;
- the first and only declaration mutation attempt succeeds;
- exactly one selected `--graph` is honored;
- the command reports the absolute selected destination;
- the target folds and checks with zero new uncarried findings;
- the declaration is visible in the selected target's folded view and history;
- the original graph digest is unchanged; and
- no other graph receives the event.

Use `FAIL` for a violated condition and `INCONCLUSIVE` only when external
environment state prevents the condition from being exercised.

The intentional epoch boundary is not under test: never attempt to append an
epoch-1 event to the original unversioned graph.

## Required return

Write the result to:

```text
C:\Users\wstri\dev\portage-depot\campaigns\lsem-census\L3-RETEST-RETURN.md
```

Include:

1. overall and condition-level verdicts;
2. settled, carried, and open orientation;
3. the exact declaration and why it is no stronger than licensed;
4. exact commands and exit results for migration, declaration, and checking;
5. declaration-surface and mutation-attempt counts;
6. original and retest graph digests;
7. an unseal log;
8. claims explicitly not made; and
9. a short "believed and wrong" section.

Do not edit the prior `L3-RETURN.md`.
