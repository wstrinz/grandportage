graph: 19 models, 0 edges, 57 claims, 0 inferences
       16 of them record no ideal, so nothing algebraic can be checked there
       (`generators: []` says a model imposes no equations; omitting it says nobody wrote them down)

UNSOUND_PREMISE  DOUBT:D-E5-GAP
    MISSING_PREMISE, raised against CL-E5-CORPUS by hand.
      E5-gap, the generative bridge. E5-argument.md section 5 states that to conclude E5 one still needs an independently controlled proof that every fence-valid type generated by sections 2-4 is present in those 5,299 inputs; that bridge was not rerun, nor was the required same-method n=22 census retrodiction. The repository enumerator is specialized to the n=23 singleton frame and would be invalid at n=22 unchanged.
    -> DISCHARGE: Answer it and record `answered` with what settled it, accept it deliberately with `gp accept`, or act on it. A doubt nobody has answered is the honest state and stays visible until one of those happens.

UNSOUND_PREMISE  FAMILY:F-BARE-TYPES
    family F-BARE-TYPES declares count 5299 and names CL-E5-CORPUS, but no current ENUMERATION evidence for that claim declares `decides: BOTH` as the exact claim establishing it.
      Every 'k of N' result in this campaign divides by that N. An uncounted family makes each of them a statement about a number nobody vouched for.
    -> DISCHARGE: Record the enumeration as a claim at this family and name it in `enumeration`. The claim must be a family PREDICATE with `asserts_count` equal to the family's count, backed by current ENUMERATION evidence with `decides: BOTH` -- how the members were counted, and how you know the count is complete. One census verified its own by checking orbit sizes summed to the labelled total; that is exactly the claim this field wants.

UNSOUND_PREMISE  FAMILY:F-V4-ACTIONS
    family F-V4-ACTIONS declares count 5395 and names CL-E5, but no current ENUMERATION evidence for that claim declares `decides: BOTH` as the exact claim establishing it.
      Every 'k of N' result in this campaign divides by that N. An uncounted family makes each of them a statement about a number nobody vouched for.
    -> DISCHARGE: Record the enumeration as a claim at this family and name it in `enumeration`. The claim must be a family PREDICATE with `asserts_count` equal to the family's count, backed by current ENUMERATION evidence with `decides: BOTH` -- how the members were counted, and how you know the count is complete. One census verified its own by checking orbit sizes summed to the labelled total; that is exactly the claim this field wants.

TRIAGE  ASSERTED-WITNESS:untested:CL-E1-2
    NONEMPTY claim CL-E1-2 records its point and nothing has substituted it.
      The cheapest check in the system: evaluating the generators at a point has no interpretation to argue about, no ordering assumption and no field subtlety. Either they all vanish or one of them does not.
    -> DISCHARGE: Run `gp verify`. It substitutes every structured witness and records the verdict, and a NOT_A_POINT answer would mean the existence claim is unsupported at its own model.

clean inferences (0): 

3 finding(s) at or above UNSOUND_PREMISE: 3 LIVE, 0 carried
--- exit: 1 ---
